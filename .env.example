# OpenRouter API Key (get free at https://openrouter.ai/)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model (optional, defaults to free model)
# Options: google/gemma-3-27b-it:free, meta-llama/llama-3.3-70b-instruct:free
OPENROUTER_MODEL=google/gemma-3-27b-it:free

# Crawl settings (optional)
MAX_CRAWL_DEPTH=3
MAX_PAGES=100

# Fast mode: skip LLM processing for much faster crawling (~5s vs ~30s per page)
# Set to "true" to enable. Uses basic content filtering instead of LLM.
FAST_MODE=false
